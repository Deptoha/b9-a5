// ..no1

const button1 = document.getElementById('button1');

button1.addEventListener('click', () => {
    button1.style.backgroundColor = '#1dd100';

    // selectedSeat

    const current = document.getElementById('selected-seat');
    const currentSeat = current.innerText;
    const currentSeatNumber = parseInt(currentSeat);
    console.log(currentSeatNumber);

    const newSeatNumber = currentSeatNumber + 1;

    current.innerText = newSeatNumber

    // totalBTd

    const total = document.getElementById('total-price')
    const totalPrice = total.innerText;
    const totalPrice1 = parseInt(totalPrice);
    console.log(totalPrice1);

    const newTotalPrice1 = newSeatNumber * 550;

    total.innerText = newTotalPrice1


    
    // grand total price

    const discountInfo = document.getElementById('price')
    const totalPricE = discountInfo.innerText;
    const totalPrice12 = parseInt(totalPricE);
    console.log(totalPrice12);

    const newTotalPrice = newTotalPrice1;

    discountInfo.innerText = newTotalPrice


    //  seat left

    const currentLeft = document.getElementById('seat-left');
    const currentSeatLeft = currentLeft.innerText;
    const currentSeatNumberLeft = parseInt(currentSeatLeft);
    console.log(currentSeatNumberLeft);

    const newSeatNumberLeft = currentSeatNumberLeft - 1;

    currentLeft.innerText = newSeatNumberLeft

});

// ...No2

const button2 = document.getElementById('button2');

button2.addEventListener('click', () => {
    button2.style.backgroundColor = '#1dd100';

    // selectedSeat

    const current = document.getElementById('selected-seat');
    const currentSeat = current.innerText;
    const currentSeatNumber = parseInt(currentSeat);
    console.log(currentSeatNumber);

    const newSeatNumber = currentSeatNumber + 1;

    current.innerText = newSeatNumber

    // totalBTd

    const total = document.getElementById('total-price')
    const totalPrice = total.innerText;
    const totalPrice1 = parseInt(totalPrice);
    console.log(totalPrice1);

    const newTotalPrice1 = newSeatNumber * 550;

    total.innerText = newTotalPrice1

    // grand total price

    const discountInfo = document.getElementById('price')
    const totalPricE = discountInfo.innerText;
    const totalPrice12 = parseInt(totalPricE);
    console.log(totalPrice12);

    const newTotalPrice = newTotalPrice1;

    discountInfo.innerText = newTotalPrice


    //  seat left

    const currentLeft = document.getElementById('seat-left');
    const currentSeatLeft = currentLeft.innerText;
    const currentSeatNumberLeft = parseInt(currentSeatLeft);
    console.log(currentSeatNumberLeft);

    const newSeatNumberLeft = currentSeatNumberLeft - 1;

    currentLeft.innerText = newSeatNumberLeft
});

// ...No3

const button3 = document.getElementById('button3');

button3.addEventListener('click', () => {
    button3.style.backgroundColor = '#1dd100';

    // selectedSeat

    const current = document.getElementById('selected-seat');
    const currentSeat = current.innerText;
    const currentSeatNumber = parseInt(currentSeat);
    console.log(currentSeatNumber);

    const newSeatNumber = currentSeatNumber + 1;

    current.innerText = newSeatNumber

    // totalBTd

    const total = document.getElementById('total-price')
    const totalPrice = total.innerText;
    const totalPrice1 = parseInt(totalPrice);
    console.log(totalPrice1);

    const newTotalPrice1 = newSeatNumber * 550;

    total.innerText = newTotalPrice1

    // grand total price

    const discountInfo = document.getElementById('price')
    const totalPricE = discountInfo.innerText;
    const totalPrice12 = parseInt(totalPricE);
    console.log(totalPrice12);

    const newTotalPrice = newTotalPrice1;

    discountInfo.innerText = newTotalPrice


    //  seat left

    const currentLeft = document.getElementById('seat-left');
    const currentSeatLeft = currentLeft.innerText;
    const currentSeatNumberLeft = parseInt(currentSeatLeft);
    console.log(currentSeatNumberLeft);

    const newSeatNumberLeft = currentSeatNumberLeft - 1;

    currentLeft.innerText = newSeatNumberLeft
});

//......No4

const button4 = document.getElementById('button4');
button4.addEventListener('click', () => {
    button4.style.backgroundColor = '#1dd100';

    // selectedSeat

    const current = document.getElementById('selected-seat');
    const currentSeat = current.innerText;
    const currentSeatNumber = parseInt(currentSeat);
    console.log(currentSeatNumber);

    const newSeatNumber = currentSeatNumber + 1;

    current.innerText = newSeatNumber

    // totalBTd

    const total = document.getElementById('total-price')
    const totalPrice = total.innerText;
    const totalPrice1 = parseInt(totalPrice);
    console.log(totalPrice1);

    const newTotalPrice1 = newSeatNumber * 550;

    total.innerText = newTotalPrice1

    // grand total price

    const discountInfo = document.getElementById('price')
    const totalPricE = discountInfo.innerText;
    const totalPrice12 = parseInt(totalPricE);
    console.log(totalPrice12);

    const newTotalPrice = newTotalPrice1;

    discountInfo.innerText = newTotalPrice


    //  seat left

    const currentLeft = document.getElementById('seat-left');
    const currentSeatLeft = currentLeft.innerText;
    const currentSeatNumberLeft = parseInt(currentSeatLeft);
    console.log(currentSeatNumberLeft);

    const newSeatNumberLeft = currentSeatNumberLeft - 1;

    currentLeft.innerText = newSeatNumberLeft
});

// ......No5

const button5 = document.getElementById('button5');

button5.addEventListener('click', () => {
    button5.style.backgroundColor = '#1dd100';

    // selectedSeat

    const current = document.getElementById('selected-seat');
    const currentSeat = current.innerText;
    const currentSeatNumber = parseInt(currentSeat);
    console.log(currentSeatNumber);

    const newSeatNumber = currentSeatNumber + 1;

    current.innerText = newSeatNumber

    // totalBTd

    const total = document.getElementById('total-price')
    const totalPrice = total.innerText;
    const totalPrice1 = parseInt(totalPrice);
    console.log(totalPrice1);

    const newTotalPrice1 = newSeatNumber * 550;

    total.innerText = newTotalPrice1

    // grand total price

    const discountInfo = document.getElementById('price')
    const totalPricE = discountInfo.innerText;
    const totalPrice12 = parseInt(totalPricE);
    console.log(totalPrice12);

    const newTotalPrice = newTotalPrice1;

    discountInfo.innerText = newTotalPrice


    //  seat left

    const currentLeft = document.getElementById('seat-left');
    const currentSeatLeft = currentLeft.innerText;
    const currentSeatNumberLeft = parseInt(currentSeatLeft);
    console.log(currentSeatNumberLeft);

    const newSeatNumberLeft = currentSeatNumberLeft - 1;

    currentLeft.innerText = newSeatNumberLeft
});

// ......No6

const button6 = document.getElementById('button6');

button6.addEventListener('click', () => {
    button6.style.backgroundColor = '#1dd100';

    // selectedSeat

    const current = document.getElementById('selected-seat');
    const currentSeat = current.innerText;
    const currentSeatNumber = parseInt(currentSeat);
    console.log(currentSeatNumber);

    const newSeatNumber = currentSeatNumber + 1;

    current.innerText = newSeatNumber

    // totalBTd

    const total = document.getElementById('total-price')
    const totalPrice = total.innerText;
    const totalPrice1 = parseInt(totalPrice);
    console.log(totalPrice1);

    const newTotalPrice1 = newSeatNumber * 550;

    total.innerText = newTotalPrice1

    // grand total price

    const discountInfo = document.getElementById('price')
    const totalPricE = discountInfo.innerText;
    const totalPrice12 = parseInt(totalPricE);
    console.log(totalPrice12);

    const newTotalPrice = newTotalPrice1;

    discountInfo.innerText = newTotalPrice


    //  seat left

    const currentLeft = document.getElementById('seat-left');
    const currentSeatLeft = currentLeft.innerText;
    const currentSeatNumberLeft = parseInt(currentSeatLeft);
    console.log(currentSeatNumberLeft);

    const newSeatNumberLeft = currentSeatNumberLeft - 1;

    currentLeft.innerText = newSeatNumberLeft
});

// ......No7

const button7 = document.getElementById('button7');

button7.addEventListener('click', () => {
    button7.style.backgroundColor = '#1dd100';


    // selectedSeat

    const current = document.getElementById('selected-seat');
    const currentSeat = current.innerText;
    const currentSeatNumber = parseInt(currentSeat);
    console.log(currentSeatNumber);

    const newSeatNumber = currentSeatNumber + 1;

    current.innerText = newSeatNumber

    // totalBTd

    const total = document.getElementById('total-price')
    const totalPrice = total.innerText;
    const totalPrice1 = parseInt(totalPrice);
    console.log(totalPrice1);

    const newTotalPrice1 = newSeatNumber * 550;

    total.innerText = newTotalPrice1

    // grand total price

    const discountInfo = document.getElementById('price')
    const totalPricE = discountInfo.innerText;
    const totalPrice12 = parseInt(totalPricE);
    console.log(totalPrice12);

    const newTotalPrice = newTotalPrice1;

    discountInfo.innerText = newTotalPrice


    //  seat left

    const currentLeft = document.getElementById('seat-left');
    const currentSeatLeft = currentLeft.innerText;
    const currentSeatNumberLeft = parseInt(currentSeatLeft);
    console.log(currentSeatNumberLeft);

    const newSeatNumberLeft = currentSeatNumberLeft - 1;

    currentLeft.innerText = newSeatNumberLeft
});

// ......No8

const button8 = document.getElementById('button8');

button8.addEventListener('click', () => {
    button8.style.backgroundColor = '#1dd100';

    // selectedSeat

    const current = document.getElementById('selected-seat');
    const currentSeat = current.innerText;
    const currentSeatNumber = parseInt(currentSeat);
    console.log(currentSeatNumber);

    const newSeatNumber = currentSeatNumber + 1;

    current.innerText = newSeatNumber

    // totalBTd

    const total = document.getElementById('total-price')
    const totalPrice = total.innerText;
    const totalPrice1 = parseInt(totalPrice);
    console.log(totalPrice1);

    const newTotalPrice1 = newSeatNumber * 550;

    total.innerText = newTotalPrice1

    // grand total price

    const discountInfo = document.getElementById('price')
    const totalPricE = discountInfo.innerText;
    const totalPrice12 = parseInt(totalPricE);
    console.log(totalPrice12);

    const newTotalPrice = newTotalPrice1;

    discountInfo.innerText = newTotalPrice


    //  seat left

    const currentLeft = document.getElementById('seat-left');
    const currentSeatLeft = currentLeft.innerText;
    const currentSeatNumberLeft = parseInt(currentSeatLeft);
    console.log(currentSeatNumberLeft);

    const newSeatNumberLeft = currentSeatNumberLeft - 1;

    currentLeft.innerText = newSeatNumberLeft
});